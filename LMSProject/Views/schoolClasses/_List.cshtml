@model IEnumerable<LMSProject.Models.schoolClass>

@{
    ViewBag.Title = "Index";
}

<p>
    @Html.ActionLink("Create New", "Create", null, new {@class ="btn btn-default"})
</p>
<table class="">
    <tr>
<!--         <th class="label label-info">
            @Html.DisplayNameFor(model => model.name)
        </th> -->
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
<!--         <td class="btn btn-info">
            @Html.DisplayFor(modelItem => item.name)
        </td> -->
        <td>
        @Html.ActionLink(item.name, "Edit", new { id=item.schoolClassID }, new {@class ="btn btn-default wide-button"})
        </td>
        <td>
            @Html.ActionLink("Delete", "Delete", new { id=item.schoolClassID }, new {@class ="btn btn-danger"})
        </td>
    </tr>
}

</table>
