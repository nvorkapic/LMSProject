@model LMSProject.Models.user

@using (Html.BeginForm("Add", "users", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    <input type="hidden" name="HiddenUserId" value="@ViewBag.HiddenUserId" />
    <input type="hidden" name="HiddenRoleId" value="@ViewBag.HiddenRoleId" />

    @Html.DropDownListFor(model => model.schoolClassID, (IEnumerable<SelectListItem>)ViewBag.SchoolClassSelectList, htmlAttributes: new { @class = "dropdown" })
    <input type="submit" value="Add" />
}